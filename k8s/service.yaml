apiVersion: v1
kind: Service
metadata:
  name: model-inference-service
  labels:
    app: model-inference-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: model-inference-service